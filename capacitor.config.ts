import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ooota-food-delivery-app-cap',
  webDir: 'dist'
};

export default config;
